<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
<title>5S CR3 — Desvios</title>
<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
<style>
  :root {
    --verde:    #1A6B3C;
    --verde-cl: #2E8B57;
    --verde-bg: #E8F5EE;
    --cinza:    #1C1C1E;
    --cinza-md: #6B7280;
    --cinza-cl: #F2F4F6;
    --branco:   #FFFFFF;
    --amarelo:  #F59E0B;
    --vermelho: #DC2626;
    --azul:     #2563EB;
    --radius:   12px;
    --shadow:   0 2px 12px rgba(0,0,0,0.08);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background: var(--cinza-cl);
    color: var(--cinza);
    min-height: 100vh;
  }

  /* ── HEADER ── */
  .app-header {
    background: var(--verde);
    color: var(--branco);
    padding: 14px 20px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: 0 2px 8px rgba(0,0,0,0.2);
  }
  .app-header h1 { font-size: 17px; font-weight: 700; letter-spacing: -0.3px; }
  .app-header .area-badge {
    background: rgba(255,255,255,0.2);
    border-radius: 20px;
    padding: 4px 12px;
    font-size: 12px;
    font-weight: 600;
    white-space: nowrap;
  }

  /* ── NAV TABS ── */
  .nav-tabs {
    display: flex;
    background: var(--branco);
    border-bottom: 1px solid #E5E7EB;
    overflow-x: auto;
    scrollbar-width: none;
  }
  .nav-tabs::-webkit-scrollbar { display: none; }
  .nav-tab {
    flex: 1;
    min-width: 100px;
    padding: 12px 8px;
    text-align: center;
    font-size: 13px;
    font-weight: 600;
    color: var(--cinza-md);
    cursor: pointer;
    border-bottom: 3px solid transparent;
    transition: all 0.2s;
    white-space: nowrap;
  }
  .nav-tab.active {
    color: var(--verde);
    border-bottom-color: var(--verde);
  }

  /* ── VIEWS ── */
  .view { display: none; padding: 16px; max-width: 480px; margin: 0 auto; }
  .view.active { display: block; }

  /* ── CARDS ── */
  .card {
    background: var(--branco);
    border-radius: var(--radius);
    padding: 16px;
    margin-bottom: 12px;
    box-shadow: var(--shadow);
  }
  .card-label {
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.8px;
    color: var(--cinza-md);
    margin-bottom: 6px;
  }

  /* ── FORMULÁRIO ── */
  .form-group { margin-bottom: 16px; }
  .form-group label {
    display: block;
    font-size: 13px;
    font-weight: 600;
    color: var(--cinza-md);
    margin-bottom: 6px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }
  input[type="text"], select, textarea {
    width: 100%;
    padding: 12px 14px;
    border: 2px solid #E5E7EB;
    border-radius: 8px;
    font-size: 15px;
    color: var(--cinza);
    background: var(--branco);
    transition: border-color 0.2s;
    -webkit-appearance: none;
  }
  input[type="text"]:focus, select:focus, textarea:focus {
    outline: none;
    border-color: var(--verde);
  }
  textarea { resize: none; height: 80px; }

  /* ── TIPO DESVIO (toggle) ── */
  .tipo-toggle {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }
  .tipo-btn {
    padding: 14px 8px;
    border: 2px solid #E5E7EB;
    border-radius: 10px;
    background: var(--branco);
    font-size: 14px;
    font-weight: 600;
    color: var(--cinza-md);
    cursor: pointer;
    text-align: center;
    transition: all 0.2s;
  }
  .tipo-btn .icon { font-size: 22px; display: block; margin-bottom: 4px; }
  .tipo-btn.selected-org { border-color: var(--amarelo); color: #92400E; background: #FFFBEB; }
  .tipo-btn.selected-limp { border-color: var(--azul); color: #1E40AF; background: #EFF6FF; }

  /* ── FOTO ── */
  .foto-area {
    border: 2px dashed #D1D5DB;
    border-radius: 10px;
    padding: 24px;
    text-align: center;
    cursor: pointer;
    transition: all 0.2s;
    background: var(--cinza-cl);
    position: relative;
  }
  .foto-area:hover { border-color: var(--verde); background: var(--verde-bg); }
  .foto-area input[type="file"] {
    position: absolute; inset: 0; opacity: 0; width: 100%; height: 100%; cursor: pointer;
  }
  .foto-area .foto-icon { font-size: 36px; margin-bottom: 8px; }
  .foto-area p { font-size: 13px; color: var(--cinza-md); }
  .foto-preview {
    width: 100%;
    max-height: 220px;
    object-fit: cover;
    border-radius: 8px;
    display: none;
    margin-top: 10px;
  }

  /* ── SUBÁREA SELECTOR ── */
  .subarea-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 10px;
  }
  .subarea-btn {
    padding: 16px;
    border: 2px solid #E5E7EB;
    border-radius: 10px;
    background: var(--branco);
    font-size: 15px;
    font-weight: 600;
    color: var(--cinza);
    cursor: pointer;
    text-align: left;
    display: flex;
    align-items: center;
    gap: 12px;
    transition: all 0.2s;
  }
  .subarea-btn:hover { border-color: var(--verde); background: var(--verde-bg); }
  .subarea-btn .floor-badge {
    background: var(--verde);
    color: white;
    border-radius: 8px;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 18px;
    font-weight: 800;
    flex-shrink: 0;
  }

  /* ── BOTÃO PRIMÁRIO ── */
  .btn-primary {
    width: 100%;
    padding: 15px;
    background: var(--verde);
    color: white;
    border: none;
    border-radius: 10px;
    font-size: 16px;
    font-weight: 700;
    cursor: pointer;
    transition: background 0.2s;
    letter-spacing: -0.2px;
  }
  .btn-primary:hover { background: var(--verde-cl); }
  .btn-primary:disabled { background: #9CA3AF; cursor: not-allowed; }

  .btn-secondary {
    width: 100%;
    padding: 13px;
    background: transparent;
    color: var(--verde);
    border: 2px solid var(--verde);
    border-radius: 10px;
    font-size: 15px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
    margin-top: 8px;
  }
  .btn-secondary:hover { background: var(--verde-bg); }

  /* ── DESVIO CARD (lista) ── */
  .desvio-card {
    background: var(--branco);
    border-radius: var(--radius);
    padding: 14px;
    margin-bottom: 10px;
    box-shadow: var(--shadow);
    border-left: 4px solid var(--cinza-cl);
  }
  .desvio-card.org { border-left-color: var(--amarelo); }
  .desvio-card.limp { border-left-color: var(--azul); }
  .desvio-card .dc-top {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 8px;
    margin-bottom: 8px;
  }
  .desvio-card .dc-meta { font-size: 12px; color: var(--cinza-md); }
  .desvio-card .dc-tipo {
    font-size: 11px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    padding: 3px 8px;
    border-radius: 20px;
    white-space: nowrap;
    flex-shrink: 0;
  }
  .dc-tipo.org { background: #FFFBEB; color: #92400E; }
  .dc-tipo.limp { background: #EFF6FF; color: #1E40AF; }

  .desvio-card .dc-area {
    font-size: 13px;
    font-weight: 700;
    color: var(--verde);
    margin-bottom: 2px;
  }
  .desvio-card .dc-obs { font-size: 13px; color: var(--cinza); margin-bottom: 8px; }
  .desvio-card .dc-foto {
    width: 100%;
    max-height: 180px;
    object-fit: cover;
    border-radius: 8px;
    margin-bottom: 10px;
    display: none;
  }
  .desvio-card .dc-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
  }
  .status-badge {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    padding: 5px 10px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 700;
  }
  .status-badge.aberto { background: #FEF2F2; color: var(--vermelho); }
  .status-badge.andamento { background: #FFFBEB; color: #92400E; }
  .status-badge.tratado { background: var(--verde-bg); color: var(--verde); }

  .btn-avancar {
    padding: 7px 14px;
    border-radius: 8px;
    border: none;
    font-size: 12px;
    font-weight: 700;
    cursor: pointer;
    transition: all 0.2s;
    background: var(--cinza-cl);
    color: var(--cinza);
  }
  .btn-avancar:hover { background: var(--verde-bg); color: var(--verde); }
  .btn-avancar:disabled { opacity: 0.4; cursor: not-allowed; }

  /* ── FILTROS ── */
  .filtros {
    display: flex;
    gap: 8px;
    margin-bottom: 14px;
    flex-wrap: wrap;
  }
  .filtro-btn {
    padding: 6px 14px;
    border-radius: 20px;
    border: 2px solid #E5E7EB;
    background: white;
    font-size: 12px;
    font-weight: 600;
    color: var(--cinza-md);
    cursor: pointer;
    transition: all 0.2s;
  }
  .filtro-btn.active { background: var(--verde); border-color: var(--verde); color: white; }

  /* ── DASHBOARD ── */
  .stats-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
    margin-bottom: 14px;
  }
  .stat-card {
    background: var(--branco);
    border-radius: var(--radius);
    padding: 14px;
    box-shadow: var(--shadow);
    text-align: center;
  }
  .stat-card .stat-num {
    font-size: 32px;
    font-weight: 800;
    color: var(--verde);
    line-height: 1;
  }
  .stat-card .stat-label {
    font-size: 11px;
    color: var(--cinza-md);
    margin-top: 4px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    font-weight: 600;
  }

  /* ── QR PANEL ── */
  .qr-grid { display: grid; gap: 16px; }
  .qr-item {
    background: var(--branco);
    border-radius: var(--radius);
    padding: 20px;
    box-shadow: var(--shadow);
    text-align: center;
  }
  .qr-item h3 { font-size: 15px; font-weight: 700; margin-bottom: 4px; }
  .qr-item p { font-size: 12px; color: var(--cinza-md); margin-bottom: 14px; }
  .qr-container { display: flex; justify-content: center; }
  .qr-container canvas, .qr-container img { border-radius: 8px; }

  /* ── TOAST ── */
  .toast {
    position: fixed;
    bottom: 24px;
    left: 50%;
    transform: translateX(-50%) translateY(80px);
    background: var(--cinza);
    color: white;
    padding: 12px 24px;
    border-radius: 30px;
    font-size: 14px;
    font-weight: 600;
    z-index: 999;
    transition: transform 0.3s ease;
    white-space: nowrap;
    max-width: 90vw;
    text-align: center;
  }
  .toast.show { transform: translateX(-50%) translateY(0); }
  .toast.success { background: var(--verde); }
  .toast.error { background: var(--vermelho); }

  /* ── CONFIG ── */
  .config-section { margin-bottom: 20px; }
  .config-section h3 { font-size: 14px; font-weight: 700; color: var(--cinza-md); text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 10px; }

  /* ── LOADING ── */
  .loading { text-align: center; padding: 40px; color: var(--cinza-md); }
  .spinner {
    width: 32px; height: 32px;
    border: 3px solid #E5E7EB;
    border-top-color: var(--verde);
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
    margin: 0 auto 12px;
  }
  @keyframes spin { to { transform: rotate(360deg); } }

  .empty-state { text-align: center; padding: 48px 16px; color: var(--cinza-md); }
  .empty-state .empty-icon { font-size: 48px; margin-bottom: 12px; }
  .empty-state p { font-size: 14px; }

  /* ── SETUP SCREEN ── */
  #setup-screen {
    position: fixed; inset: 0;
    background: var(--cinza-cl);
    z-index: 200;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 24px;
  }
  .setup-box {
    background: white;
    border-radius: 16px;
    padding: 28px;
    width: 100%;
    max-width: 400px;
    box-shadow: 0 8px 32px rgba(0,0,0,0.12);
  }
  .setup-box .logo { font-size: 40px; text-align: center; margin-bottom: 8px; }
  .setup-box h2 { font-size: 20px; font-weight: 800; text-align: center; margin-bottom: 4px; }
  .setup-box p { font-size: 13px; color: var(--cinza-md); text-align: center; margin-bottom: 20px; }
  .setup-box .setup-note {
    background: #FEF3C7;
    border-radius: 8px;
    padding: 10px 12px;
    font-size: 12px;
    color: #92400E;
    margin-bottom: 16px;
  }
</style>
</head>
<body>

<!-- ── SETUP SCREEN ── -->
<div id="setup-screen">
  <div class="setup-box">
    <div class="logo">🏭</div>
    <h2>5S CR3 — Desvios</h2>
    <p>Configure a conexão com o Supabase para começar</p>
    <div class="setup-note">
      ⚠️ Cole as credenciais do seu projeto Supabase. Elas ficam salvas no navegador.
    </div>
    <div class="form-group">
      <label>Supabase URL</label>
      <input type="text" id="cfg-url" placeholder="https://xxxx.supabase.co">
    </div>
    <div class="form-group">
      <label>Anon Key</label>
      <input type="text" id="cfg-key" placeholder="eyJhbGci...">
    </div>
    <button class="btn-primary" onclick="salvarConfig()">Conectar</button>
  </div>
</div>

<!-- ── APP SHELL ── -->
<div id="app-shell" style="display:none;">
  <header class="app-header">
    <h1>🏭 5S CR3</h1>
    <span class="area-badge" id="header-area">—</span>
  </header>

  <nav class="nav-tabs">
    <div class="nav-tab active" onclick="showView('registrar')">＋ Registrar</div>
    <div class="nav-tab" onclick="showView('lista')">📋 Desvios</div>
    <div class="nav-tab" onclick="showView('dashboard')">📊 Painel</div>
    <div class="nav-tab" onclick="showView('qrcodes')">QR Codes</div>
    <div class="nav-tab" onclick="showView('config')">⚙️</div>
  </nav>

  <!-- VIEW: REGISTRAR -->
  <div class="view active" id="view-registrar">

    <!-- Seleção de subárea (quando não vem por QR) -->
    <div id="subarea-picker" style="display:none;">
      <div class="card">
        <div class="card-label">Selecione a subárea</div>
        <div class="subarea-grid">
          <button class="subarea-btn" onclick="selecionarArea('piso0-cr3')">
            <div class="floor-badge">0</div>
            <div><div style="font-size:15px;font-weight:700;">Piso 0 CR3</div><div style="font-size:12px;color:var(--cinza-md);">Nível térreo</div></div>
          </button>
          <button class="subarea-btn" onclick="selecionarArea('piso1-cr3')">
            <div class="floor-badge">1</div>
            <div><div style="font-size:15px;font-weight:700;">Piso 1 CR3</div><div style="font-size:12px;color:var(--cinza-md);">Primeiro andar</div></div>
          </button>
          <button class="subarea-btn" onclick="selecionarArea('piso2-cr3')">
            <div class="floor-badge">2</div>
            <div><div style="font-size:15px;font-weight:700;">Piso 2 CR3</div><div style="font-size:12px;color:var(--cinza-md);">Segundo andar</div></div>
          </button>
        </div>
      </div>
    </div>

    <!-- Formulário principal -->
    <div id="form-desvio" style="display:none;">

      <div class="card">
        <div class="card-label">Responsável</div>
        <div class="form-group" style="margin-bottom:10px;">
          <label>Nome</label>
          <input type="text" id="reg-nome" placeholder="Seu nome completo">
        </div>
        <div class="form-group" style="margin-bottom:0;">
          <label>Matrícula</label>
          <input type="text" id="reg-matricula" placeholder="Ex.: 123456">
        </div>
      </div>

      <div class="card">
        <div class="card-label">Tipo de Desvio</div>
        <div class="tipo-toggle">
          <button class="tipo-btn" id="btn-org" onclick="selectTipo('organizacao')">
            <span class="icon">📦</span> Organização
          </button>
          <button class="tipo-btn" id="btn-limp" onclick="selectTipo('limpeza')">
            <span class="icon">🧹</span> Limpeza
          </button>
        </div>
      </div>

      <div class="card">
        <div class="card-label">Foto do Desvio</div>
        <label style="display:block;background:var(--verde-bg);border:2px dashed var(--verde);border-radius:10px;padding:28px 16px;text-align:center;cursor:pointer;font-size:15px;font-weight:600;color:var(--verde);">
          📷 &nbsp;Tirar foto / escolher da galeria
          <input type="file" accept="image/*" onchange="previewFoto(this)" style="display:block;width:100%;margin-top:12px;">
        </label>
        <img class="foto-preview" id="foto-preview" alt="Preview">
      </div>

      <div class="card">
        <div class="card-label">Observação (opcional)</div>
        <textarea id="reg-obs" placeholder="Descreva o desvio brevemente..."></textarea>
      </div>

      <button class="btn-primary" id="btn-registrar" onclick="registrarDesvio()">Registrar Desvio</button>
      <button class="btn-secondary" onclick="voltarSubarea()">← Trocar subárea</button>
      <div style="height:24px;"></div>
    </div>
  </div>

  <!-- VIEW: LISTA -->
  <div class="view" id="view-lista">
    <div class="filtros">
      <button class="filtro-btn active" onclick="setFiltro('todos',this)">Todos</button>
      <button class="filtro-btn" onclick="setFiltro('aberto',this)">Abertos</button>
      <button class="filtro-btn" onclick="setFiltro('andamento',this)">Em andamento</button>
      <button class="filtro-btn" onclick="setFiltro('tratado',this)">Tratados</button>
    </div>
    <div class="filtros" style="margin-top:-6px;">
      <button class="filtro-btn active" onclick="setFiltroArea('todas',this)">Todas as áreas</button>
      <button class="filtro-btn" onclick="setFiltroArea('piso0-cr3',this)">Piso 0</button>
      <button class="filtro-btn" onclick="setFiltroArea('piso1-cr3',this)">Piso 1</button>
      <button class="filtro-btn" onclick="setFiltroArea('piso2-cr3',this)">Piso 2</button>
    </div>
    <div id="lista-container">
      <div class="loading"><div class="spinner"></div><p>Carregando...</p></div>
    </div>
  </div>

  <!-- VIEW: DASHBOARD -->
  <div class="view" id="view-dashboard">
    <div class="stats-grid">
      <div class="stat-card">
        <div class="stat-num" id="stat-total">—</div>
        <div class="stat-label">Total</div>
      </div>
      <div class="stat-card">
        <div class="stat-num" id="stat-abertos" style="color:var(--vermelho);">—</div>
        <div class="stat-label">Abertos</div>
      </div>
      <div class="stat-card">
        <div class="stat-num" id="stat-andamento" style="color:var(--amarelo);">—</div>
        <div class="stat-label">Em andamento</div>
      </div>
      <div class="stat-card">
        <div class="stat-num" id="stat-tratados" style="color:var(--verde);">—</div>
        <div class="stat-label">Tratados</div>
      </div>
    </div>

    <div class="card">
      <div class="card-label">Por subárea</div>
      <div id="dash-areas"></div>
    </div>

    <div class="card">
      <div class="card-label">Por tipo</div>
      <div id="dash-tipos"></div>
    </div>
  </div>

  <!-- VIEW: QR CODES -->
  <div class="view" id="view-qrcodes">
    <div class="card" style="margin-bottom:14px;">
      <div class="card-label">URL base</div>
      <input type="text" id="qr-base-url" placeholder="https://seu-usuario.github.io/seu-repo" style="margin-bottom:8px;">
      <button class="btn-primary" onclick="gerarQRs()">Gerar QR Codes</button>
    </div>
    <div class="qr-grid" id="qr-grid"></div>
  </div>

  <!-- VIEW: CONFIG -->
  <div class="view" id="view-config">
    <div class="config-section">
      <h3>Conexão Supabase</h3>
      <div class="card">
        <div class="form-group" style="margin-bottom:10px;">
          <label>URL</label>
          <input type="text" id="cfg-url2" placeholder="https://xxxx.supabase.co">
        </div>
        <div class="form-group" style="margin-bottom:10px;">
          <label>Anon Key</label>
          <input type="text" id="cfg-key2" placeholder="eyJhbGci...">
        </div>
        <button class="btn-primary" onclick="salvarConfig2()">Salvar Credenciais</button>
      </div>
    </div>

    <div class="config-section">
      <h3>Banco de dados</h3>
      <div class="card">
        <p style="font-size:13px;color:var(--cinza-md);margin-bottom:12px;">Execute este SQL no Supabase SQL Editor para criar as tabelas necessárias:</p>
        <textarea id="sql-script" style="font-family:monospace;font-size:11px;height:220px;background:#1C1C1E;color:#4ADE80;border:none;border-radius:8px;padding:12px;" readonly></textarea>
        <button class="btn-secondary" onclick="copiarSQL()">Copiar SQL</button>
      </div>
    </div>

    <div class="config-section">
      <h3>Storage</h3>
      <div class="card">
        <p style="font-size:13px;color:var(--cinza-md);">Crie um bucket chamado <strong>fotos-desvios</strong> no Supabase Storage com acesso público habilitado.</p>
      </div>
    </div>
  </div>
</div>

<!-- TOAST -->
<div class="toast" id="toast"></div>

<script>
// ══════════════════════════════════════════
//  CONFIG & INIT
// ══════════════════════════════════════════
const SQL_SETUP = `-- Execute no SQL Editor do Supabase

create table if not exists desvios (
  id          uuid primary key default gen_random_uuid(),
  area        text not null,
  tipo        text not null check (tipo in ('organizacao','limpeza')),
  status      text not null default 'aberto' check (status in ('aberto','andamento','tratado')),
  nome        text not null,
  matricula   text not null,
  observacao  text,
  foto_url    text,
  created_at  timestamptz default now(),
  updated_at  timestamptz default now()
);

-- Habilitar RLS
alter table desvios enable row level security;

-- Políticas abertas (acesso via QR, sem login)
create policy "leitura publica" on desvios for select using (true);
create policy "insercao publica" on desvios for insert with check (true);
create policy "atualizacao publica" on desvios for update using (true);`;

let SUPABASE_URL = '';
let SUPABASE_KEY = '';
let areaAtual = '';
let tipoSelecionado = '';
let filtroStatus = 'todos';
let filtroArea = 'todas';
let fotoFile = null;

const AREAS = {
  'piso0-cr3': 'Piso 0 CR3',
  'piso1-cr3': 'Piso 1 CR3',
  'piso2-cr3': 'Piso 2 CR3'
};

function init() {
  SUPABASE_URL = localStorage.getItem('sb_url') || '';
  SUPABASE_KEY = localStorage.getItem('sb_key') || '';

  document.getElementById('sql-script').value = SQL_SETUP;

  if (!SUPABASE_URL || !SUPABASE_KEY) {
    document.getElementById('setup-screen').style.display = 'flex';
    return;
  }

  document.getElementById('setup-screen').style.display = 'none';
  document.getElementById('app-shell').style.display = 'block';
  document.getElementById('cfg-url2').value = SUPABASE_URL;
  document.getElementById('cfg-key2').value = SUPABASE_KEY;

  // QR code URL base
  document.getElementById('qr-base-url').value = window.location.origin + window.location.pathname.replace('index.html','');

  // Detectar subárea via URL param
  const params = new URLSearchParams(window.location.search);
  const areaParam = params.get('area');
  if (areaParam && AREAS[areaParam]) {
    areaAtual = areaParam;
    atualizarHeaderArea();
    mostrarFormulario();
  } else {
    document.getElementById('subarea-picker').style.display = 'block';
    document.getElementById('form-desvio').style.display = 'none';
  }
}

function salvarConfig() {
  const url = document.getElementById('cfg-url').value.trim();
  const key = document.getElementById('cfg-key').value.trim();
  if (!url || !key) { toast('Preencha URL e Anon Key', 'error'); return; }
  localStorage.setItem('sb_url', url);
  localStorage.setItem('sb_key', key);
  location.reload();
}

function salvarConfig2() {
  const url = document.getElementById('cfg-url2').value.trim();
  const key = document.getElementById('cfg-key2').value.trim();
  if (!url || !key) { toast('Preencha URL e Anon Key', 'error'); return; }
  localStorage.setItem('sb_url', url);
  localStorage.setItem('sb_key', key);
  SUPABASE_URL = url;
  SUPABASE_KEY = key;
  toast('Credenciais salvas!', 'success');
}

function copiarSQL() {
  navigator.clipboard.writeText(SQL_SETUP).then(() => toast('SQL copiado!', 'success'));
}

// ══════════════════════════════════════════
//  NAVEGAÇÃO
// ══════════════════════════════════════════
function showView(name) {
  document.querySelectorAll('.view').forEach(v => v.classList.remove('active'));
  document.querySelectorAll('.nav-tab').forEach(t => t.classList.remove('active'));
  document.getElementById('view-' + name).classList.add('active');
  event.target.classList.add('active');

  if (name === 'lista') carregarLista();
  if (name === 'dashboard') carregarDashboard();
}

// ══════════════════════════════════════════
//  SUBÁREA
// ══════════════════════════════════════════
function selecionarArea(area) {
  areaAtual = area;
  atualizarHeaderArea();
  mostrarFormulario();
}

function atualizarHeaderArea() {
  document.getElementById('header-area').textContent = AREAS[areaAtual] || '—';
}

function mostrarFormulario() {
  document.getElementById('subarea-picker').style.display = 'none';
  document.getElementById('form-desvio').style.display = 'block';
}

function voltarSubarea() {
  areaAtual = '';
  document.getElementById('header-area').textContent = '—';
  document.getElementById('subarea-picker').style.display = 'block';
  document.getElementById('form-desvio').style.display = 'none';
  resetForm();
}

// ══════════════════════════════════════════
//  FORMULÁRIO
// ══════════════════════════════════════════
function selectTipo(tipo) {
  tipoSelecionado = tipo;
  document.getElementById('btn-org').className = 'tipo-btn' + (tipo === 'organizacao' ? ' selected-org' : '');
  document.getElementById('btn-limp').className = 'tipo-btn' + (tipo === 'limpeza' ? ' selected-limp' : '');
}

function previewFoto(input) {
  if (!input.files || !input.files[0]) return;
  fotoFile = input.files[0];
  const reader = new FileReader();
  reader.onload = e => {
    const img = document.getElementById('foto-preview');
    img.src = e.target.result;
    img.style.display = 'block';
  };
  reader.readAsDataURL(fotoFile);
}

function resetForm() {
  document.getElementById('reg-nome').value = '';
  document.getElementById('reg-matricula').value = '';
  document.getElementById('reg-obs').value = '';
  const prev = document.getElementById('foto-preview');
  prev.style.display = 'none';
  prev.src = '';
  // limpa todos os inputs de arquivo do formulário
  document.querySelectorAll('#form-desvio input[type=file]').forEach(i => i.value = '');
  fotoFile = null;
  tipoSelecionado = '';
  document.getElementById('btn-org').className = 'tipo-btn';
  document.getElementById('btn-limp').className = 'tipo-btn';
}

async function registrarDesvio() {
  const nome = document.getElementById('reg-nome').value.trim();
  const matricula = document.getElementById('reg-matricula').value.trim();
  const obs = document.getElementById('reg-obs').value.trim();

  if (!nome) { toast('Informe seu nome', 'error'); return; }
  if (!matricula) { toast('Informe sua matrícula', 'error'); return; }
  if (!tipoSelecionado) { toast('Selecione o tipo de desvio', 'error'); return; }
  if (!fotoFile) { toast('Tire uma foto do desvio', 'error'); return; }

  const btn = document.getElementById('btn-registrar');
  btn.disabled = true;
  btn.textContent = 'Enviando...';

  try {
    // 1. Upload da foto
    const ext = fotoFile.name.split('.').pop() || 'jpg';
    const fileName = `${Date.now()}-${Math.random().toString(36).slice(2)}.${ext}`;
    const uploadRes = await fetch(`${SUPABASE_URL}/storage/v1/object/fotos-desvios/${fileName}`, {
      method: 'POST',
      headers: {
        'apikey': SUPABASE_KEY,
        'Authorization': `Bearer ${SUPABASE_KEY}`,
        'Content-Type': fotoFile.type,
        'x-upsert': 'false'
      },
      body: fotoFile
    });

    let foto_url = null;
    if (uploadRes.ok) {
      foto_url = `${SUPABASE_URL}/storage/v1/object/public/fotos-desvios/${fileName}`;
    }

    // 2. Inserir desvio
    const body = { area: areaAtual, tipo: tipoSelecionado, status: 'aberto', nome, matricula, observacao: obs || null, foto_url };
    const res = await sbPost('/rest/v1/desvios', body);

    if (!res.ok) throw new Error(await res.text());

    toast('Desvio registrado!', 'success');
    resetForm();

  } catch (e) {
    toast('Erro ao registrar: ' + e.message, 'error');
  } finally {
    btn.disabled = false;
    btn.textContent = 'Registrar Desvio';
  }
}

// ══════════════════════════════════════════
//  LISTA
// ══════════════════════════════════════════
function setFiltro(status, el) {
  filtroStatus = status;
  document.querySelectorAll('.filtros')[0].querySelectorAll('.filtro-btn').forEach(b => b.classList.remove('active'));
  el.classList.add('active');
  carregarLista();
}

function setFiltroArea(area, el) {
  filtroArea = area;
  document.querySelectorAll('.filtros')[1].querySelectorAll('.filtro-btn').forEach(b => b.classList.remove('active'));
  el.classList.add('active');
  carregarLista();
}

async function carregarLista() {
  const container = document.getElementById('lista-container');
  container.innerHTML = '<div class="loading"><div class="spinner"></div><p>Carregando...</p></div>';

  try {
    let url = `${SUPABASE_URL}/rest/v1/desvios?order=created_at.desc&limit=100`;
    if (filtroStatus !== 'todos') url += `&status=eq.${filtroStatus}`;
    if (filtroArea !== 'todas') url += `&area=eq.${filtroArea}`;

    const res = await sbGet(url);
    const dados = await res.json();

    if (!dados.length) {
      container.innerHTML = '<div class="empty-state"><div class="empty-icon">✅</div><p>Nenhum desvio encontrado</p></div>';
      return;
    }

    container.innerHTML = dados.map(d => renderDesvioCard(d)).join('');
  } catch (e) {
    container.innerHTML = `<div class="empty-state"><div class="empty-icon">⚠️</div><p>Erro: ${e.message}</p></div>`;
  }
}

function renderDesvioCard(d) {
  const statusMap = {
    aberto:    { label: '🔴 Aberto',        cls: 'aberto' },
    andamento: { label: '🟡 Em andamento',  cls: 'andamento' },
    tratado:   { label: '🟢 Tratado',       cls: 'tratado' }
  };
  const s = statusMap[d.status];
  const tipoLabel = d.tipo === 'organizacao' ? '📦 Organização' : '🧹 Limpeza';
  const tipoCls   = d.tipo === 'organizacao' ? 'org' : 'limp';
  const data = new Date(d.created_at).toLocaleString('pt-BR', {day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'});

  const proximoStatus = d.status === 'aberto' ? 'andamento' : d.status === 'andamento' ? 'tratado' : null;
  const btnLabel = d.status === 'aberto' ? 'Iniciar tratamento' : d.status === 'andamento' ? 'Marcar tratado' : '';

  return `
  <div class="desvio-card ${tipoCls}" id="card-${d.id}">
    <div class="dc-top">
      <div>
        <div class="dc-area">${AREAS[d.area] || d.area}</div>
        <div class="dc-meta">${d.nome} · ${d.matricula} · ${data}</div>
      </div>
      <span class="dc-tipo ${tipoCls}">${tipoLabel}</span>
    </div>
    ${d.observacao ? `<div class="dc-obs">${d.observacao}</div>` : ''}
    ${d.foto_url ? `<img class="dc-foto" src="${d.foto_url}" style="display:block;" alt="foto desvio" onerror="this.style.display='none'">` : ''}
    <div class="dc-footer">
      <span class="status-badge ${s.cls}">${s.label}</span>
      ${proximoStatus ? `<button class="btn-avancar" onclick="avancarStatus('${d.id}','${proximoStatus}',this)">${btnLabel}</button>` : ''}
    </div>
  </div>`;
}

async function avancarStatus(id, novoStatus, btn) {
  btn.disabled = true;
  btn.textContent = '...';
  try {
    const res = await sbPatch(`/rest/v1/desvios?id=eq.${id}`, { status: novoStatus, updated_at: new Date().toISOString() });
    if (!res.ok) throw new Error(await res.text());
    toast('Status atualizado!', 'success');
    carregarLista();
  } catch(e) {
    toast('Erro: ' + e.message, 'error');
    btn.disabled = false;
  }
}

// ══════════════════════════════════════════
//  DASHBOARD
// ══════════════════════════════════════════
async function carregarDashboard() {
  try {
    const res = await sbGet(`${SUPABASE_URL}/rest/v1/desvios?select=area,tipo,status`);
    const dados = await res.json();

    document.getElementById('stat-total').textContent = dados.length;
    document.getElementById('stat-abertos').textContent = dados.filter(d => d.status === 'aberto').length;
    document.getElementById('stat-andamento').textContent = dados.filter(d => d.status === 'andamento').length;
    document.getElementById('stat-tratados').textContent = dados.filter(d => d.status === 'tratado').length;

    // Por área
    const porArea = {};
    dados.forEach(d => { porArea[d.area] = (porArea[d.area] || 0) + 1; });
    document.getElementById('dash-areas').innerHTML = Object.entries(AREAS).map(([k, v]) => {
      const n = porArea[k] || 0;
      const pct = dados.length ? Math.round(n / dados.length * 100) : 0;
      return `<div style="margin-bottom:10px;">
        <div style="display:flex;justify-content:space-between;font-size:13px;font-weight:600;margin-bottom:4px;"><span>${v}</span><span>${n}</span></div>
        <div style="background:#E5E7EB;border-radius:20px;height:8px;"><div style="background:var(--verde);border-radius:20px;height:8px;width:${pct}%;transition:width 0.4s;"></div></div>
      </div>`;
    }).join('');

    // Por tipo
    const nOrg = dados.filter(d => d.tipo === 'organizacao').length;
    const nLimp = dados.filter(d => d.tipo === 'limpeza').length;
    const total = nOrg + nLimp || 1;
    document.getElementById('dash-tipos').innerHTML = `
      <div style="margin-bottom:10px;">
        <div style="display:flex;justify-content:space-between;font-size:13px;font-weight:600;margin-bottom:4px;"><span>📦 Organização</span><span>${nOrg}</span></div>
        <div style="background:#E5E7EB;border-radius:20px;height:8px;"><div style="background:var(--amarelo);border-radius:20px;height:8px;width:${Math.round(nOrg/total*100)}%;"></div></div>
      </div>
      <div>
        <div style="display:flex;justify-content:space-between;font-size:13px;font-weight:600;margin-bottom:4px;"><span>🧹 Limpeza</span><span>${nLimp}</span></div>
        <div style="background:#E5E7EB;border-radius:20px;height:8px;"><div style="background:var(--azul);border-radius:20px;height:8px;width:${Math.round(nLimp/total*100)}%;"></div></div>
      </div>`;

  } catch(e) {
    console.error(e);
  }
}

// ══════════════════════════════════════════
//  QR CODES
// ══════════════════════════════════════════
function gerarQRs() {
  const base = document.getElementById('qr-base-url').value.trim().replace(/\/$/, '');
  if (!base) { toast('Informe a URL base', 'error'); return; }

  const grid = document.getElementById('qr-grid');
  grid.innerHTML = '';

  Object.entries(AREAS).forEach(([key, label]) => {
    const url = `${base}?area=${key}`;
    const item = document.createElement('div');
    item.className = 'qr-item';
    item.innerHTML = `<h3>${label}</h3><p>${url}</p><div class="qr-container" id="qr-${key}"></div>`;
    grid.appendChild(item);

    setTimeout(() => {
      new QRCode(document.getElementById(`qr-${key}`), {
        text: url, width: 200, height: 200,
        colorDark: '#1A6B3C', colorLight: '#ffffff',
        correctLevel: QRCode.CorrectLevel.M
      });
    }, 50);
  });
}

// ══════════════════════════════════════════
//  SUPABASE HELPERS
// ══════════════════════════════════════════
function sbHeaders() {
  return {
    'apikey': SUPABASE_KEY,
    'Authorization': `Bearer ${SUPABASE_KEY}`,
    'Content-Type': 'application/json',
    'Prefer': 'return=minimal'
  };
}

function sbGet(url) {
  const fullUrl = url.startsWith('http') ? url : SUPABASE_URL + url;
  return fetch(fullUrl, { headers: { 'apikey': SUPABASE_KEY, 'Authorization': `Bearer ${SUPABASE_KEY}` } });
}

function sbPost(path, body) {
  return fetch(SUPABASE_URL + path, { method: 'POST', headers: sbHeaders(), body: JSON.stringify(body) });
}

function sbPatch(path, body) {
  return fetch(SUPABASE_URL + path, { method: 'PATCH', headers: sbHeaders(), body: JSON.stringify(body) });
}

// ══════════════════════════════════════════
//  TOAST
// ══════════════════════════════════════════
let toastTimer;
function toast(msg, type = '') {
  const el = document.getElementById('toast');
  el.textContent = msg;
  el.className = `toast ${type} show`;
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => { el.classList.remove('show'); }, 2800);
}

// ── START ──
init();
</script>
</body>
</html>
